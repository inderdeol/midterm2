@model COMP2084MidtermW20.Models.Party

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Party</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(PartyId => PartyId.Winner)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(PartyId => PartyId.Winner)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(PartyId => PartyId.Winner)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(PartyId => PartyId.Winner)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.PartyId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
